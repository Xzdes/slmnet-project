{
  "architecture": {
    "input": 500,
    "hidden": 64,
    "output": 6
  },
  "weights": {
    "ih": [],
    "ho": [],
    "bh": [],
    "bo": []
  },
  "_comment": "The weight arrays below will be populated by a script. If you are running this without a Node.js backend, you can use the following snippet in your browser console to generate random weights and save the updated JSON. First, copy the entire content of this file into a variable, e.g., `let modelJson = { ... };`. Then run the script.",
  "_script": "const arch = modelJson.architecture; modelJson.weights.ih = Array.from({ length: arch.hidden * arch.input }, () => Math.random() * 2 - 1); modelJson.weights.ho = Array.from({ length: arch.output * arch.hidden }, () => Math.random() * 2 - 1); modelJson.weights.bh = Array.from({ length: arch.hidden }, () => Math.random() * 2 - 1); modelJson.weights.bo = Array.from({ length: arch.output }, () => Math.random() * 2 - 1); console.log(JSON.stringify(modelJson));"
}